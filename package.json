{
  "name": "microbe-video",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "libs",
    "apps/api-gateway",
    "apps/videos-service",
    "apps/auth-service",
    "apps/user-service"
  ],
  "scripts": {
    "build": "bun run build:libs && bun run build:apps",
    "build:libs": "bun run --filter './libs' build",
    "build:apps": "bun run --filter './apps/*' build",
    "start:gateway": "bun run --filter api-gateway start:dev",
    "start:videos": "bun --filter videos-service start:dev",
    "start:auth": "bun --filter auth-service start:dev",
    "start:user": "bun --filter user-service start:dev",
    "start:gateway:prod": "bun --filter api-gateway start:prod",
    "start:videos:prod": "bun --filter videos-service start:prod",
    "start:auth:prod": "bun run --filter auth-service start:prod",
    "start:user:prod": "bun run --filter user-service start:prod",
    "format": "bun run --filter './**' format",
    "lint": "bun run --filter './**' lint",
    "test": "bun run --filter './**' test"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "glob": "^11.0.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@types/node": "^25.6.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1"
  }
}
